DBMS = DB management system...that is what mongoDB is.

mongo can manage multiple DB's

NoSQL- Document Database

MongoDB Server
  Database
    Collections
      Documents
        Fields(properties)

mongoose schema => compiled into a mongoose model = bridge to documents on the database